# Database Configuration
# These are used by the Nepali Corpus scrapers and dashboard.
# Update the password to match your local PostgreSQL installation.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=nepali_corpus
DB_USER=postgres
DB_PASSWORD=postgres

# Dashboard Configuration
DASHBOARD_HOST=0.0.0.0
DASHBOARD_PORT=8000
